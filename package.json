{
  "name": "cheri-finance-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-cloudflare.mjs",
    "preview": "python3 -m http.server 8788 --directory dist",
    "worker:dev": "npx wrangler dev --config wrangler.toml",
    "worker:deploy": "npx wrangler deploy --config wrangler.toml"
  }
}
